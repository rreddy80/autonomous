These scripts are generated by AI ChatGPT

To build the Docker image, navigate to the directory containing the Dockerfile and run the following command:
# docker build -t gazebo-px4-ardupilot .

Once the image is built, you can run a container with the following command:
# docker run -it --rm --privileged -v /tmp/.X11-unix:/tmp/.X11-unix -e DISPLAY=$DISPLAY gazebo-px4-ardupilot

######
Running SITL on vagrant: https://ardupilot.org/dev/docs/setting-up-sitl-using-vagrant.html
######
download VirtualBox at https://www.virtualbox.org/wiki/Downloads
download and install vagrant by running brew install hashicorp/tap/hashicorp-vagrant

